'use strict';

// Entrypoint exclusivo da Vercel Function. A inicializacao local, incluindo
// listen(), limpeza periodica e signal handlers, permanece em server.js.
module.exports = require('../src/app');
